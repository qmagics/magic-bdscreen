import { defineComponent } from "vue";

export default defineComponent({
    setup: (props) => {
        return () => {
            return <div class="renderer">

            </div>
        }
    },
})